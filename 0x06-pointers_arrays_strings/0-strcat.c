#include "main.h"
#include <string.h>

/**
 *_ strcat - > this is a function strcat
 * @dest: first param
 * @src: second param
 *
 * Return: a string
 */

char *_strcat(char *dest, char *src)
{

	int index = 0, i;

	while (desk[len])
		len++;

	for (i = 0; src[i] != '\0', i++
			{dest[len] = src[i];
			len += 1;
			}
		strcat(dest, src)
		dest[len] = '\0';

	return (dest);
}
