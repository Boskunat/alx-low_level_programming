#include <stdio.h>
#include <stdliv.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return Aways (success)
 */
{
	char low;

	for (low = 'a'; low <= 'z'; low++)
		putchar(low);
	printf('\n');

	return (0);
}
